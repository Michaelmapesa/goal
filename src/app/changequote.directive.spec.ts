import { ChangequoteDirective } from './changequote.directive';

describe('ChangequoteDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangequoteDirective();
    expect(directive).toBeTruthy();
  });
});
